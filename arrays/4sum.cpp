#include<bits/stdc++.h>
using namespace std;


 vector<vector<int>> optimal_fourSum(vector<int>& nums, int target) {
     
    set <vector<int>> ans;
    int n=nums.size();
    for(int i=0;i<n;i++){
        for(int j=1;j<n;j++){
            if()
        }
    }

    }

